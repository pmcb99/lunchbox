# Core dependencies for lunchbox testing
# sqlite3 is in the standard library
requests~=2.31.0  # HTTP client for API testing
python-dotenv~=1.0.0  # Environment variable management

# Optional dependencies for enhanced testing
click~=8.1.0  # CLI parsing (if we build custom commands)
pytest  # Will be used later for structured tests
pytest-mock  # Mocking utilities for testing

# Database tools for validation
sqlalchemy  # For cross-database validation